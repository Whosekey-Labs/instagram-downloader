{
  "name": "open-media-downloader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "무료 오픈소스 Instagram 계정 미디어 다운로드 확장",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "preview": "node scripts/preview.mjs",
    "package": "npm test && npm run build && npm run check && node scripts/package.mjs"
  },
  "engines": {"node": ">=22"}
}
